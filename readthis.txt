Can you add a command: "python main.py add continue"?

Basically, when I run "python main.py add <url>", it starts processing. If I press Ctrl + C and interrupt it, the progress stops at that point.

I want "python main.py add continue" to resume from where it stopped, but not start all over again.

"python main.py add continue" and "python main.py add <url>" should be different:

- "python main.py add <url>" should process a new URL from the beginning
- "python main.py add continue" should resume the previous progress from where it was interrupted (without restarting from the beginning)

It should work similar to "python main.py run", where if the process stops and you run it again, it continues from where it left off. Basically queue system you know

Can you make clear queue command into 2 variant
Clear queue run and clear queue add